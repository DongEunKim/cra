# CLAUDE.md — 작업 가이드

건설기계 제어기 보안 기술문서를 **사내외 배포용 공식 문서**로 정제하고 **EU CRA**에 대응하는 문서 프로젝트.

## 원칙

- **원본(Source of Truth)은 `docs/`의 Markdown.** 배포용 HTML/PDF는 여기서 파생 생성한다.
- **`legacy/`는 편집 금지.** 원본 HTML 3종은 참조·추출 전용으로 보존한다.
- **언어는 한국어.**
- **용어는 [docs/00-공통/용어집.md](docs/00-공통/용어집.md)를 기준으로 통일**한다. 약어는 최초 등장 시 풀어쓴다.
- 문서 간 참조는 문서 ID·절 번호로 표기한다 (예: `DOC-10 §3.2`).
- 보안·규제 관련 서술은 단정하기 전에 원본 근거 또는 표준을 확인한다. 미확정은 `TODO`/`TBD`로 명시.

## 문서 체계

문서는 **Tier A(사양, 본 프로젝트 저작)**와 **Tier B(참조·CRA 배경, 컨설턴트 소관·설계자 인지용)**로 나뉜다.
전체 구조·읽는 순서·절차 배치 원칙은 [docs/README.md 문서 지도](docs/README.md)를 따른다.

| 계층 | ID | 문서 | 경로 |
|------|----|------|------|
| 공통 | DOC-00 | 용어집·참조표준 | `docs/00-공통/` |
| B(참조) | REF-01~03 | CRA 개요 / 관리체계 / 추적성·적합성 | `docs/05-참조-CRA배경/` |
| A1(사양) | DOC-10 | 시스템·장비 보안 아키텍처 | `docs/10-시스템장비-보안아키텍처/` |
| A2(사양) | DOC-20 | 업데이트 사양(FOTA·유선) | `docs/20-업데이트사양/` |
| A3(사양) | DOC-30 | 진단·인증(UDS 0x29) | `docs/30-진단-인증사양/` |
| A4(사양) | DOC-40 | 생애주기·절차 | `docs/40-생애주기-절차/` |

## 프로젝트 도구 (스킬·에이전트)

문서 작업의 일관성·품질을 위해 프로젝트 로컬 도구를 둔다 (`.claude/`).

**스킬** (편집 시 인라인 적용):
- `term-guard` — 용어를 DOC-00 기준으로 강제. PKI/FOTA/UDS 용어를 쓸 때 발동.
- `doc-house-style` — 메타블록·개정이력·상호참조·마커 등 문서 규약.
- `security-diagram` — SVG 다이어그램 하우스 스타일(자기완결·색/레인 규격).

**서브에이전트** (사용자 요청 시에만 스폰, read-only):
- `pki-security-reviewer` — 암호·PKI 정확성 심층 심사.
- `cra-compliance-analyst` — CRA 조문 ↔ 문서 매핑·갭 분석 (web 접근).
- `korean-style-reviewer` — 문장·문체 자연스러움 검수(번역투·비유 등), 자연스러운 대안 제시.

## 정제 시 주의 (원본에서 확인된 결함)

- 오탈자: "패키"(→패키지) 등. SVG 다이어그램 내 텍스트 잘림 다수.
- 버전 불일치(v5.1/v5.2/v1.2) → 정제 문서는 새 버전 체계로 재기산.
- fota_v5.2는 fota_v5.1의 재설명·중복이 많음 → DOC-10에 통합하되 중복 제거.
- 외부 의존(Tailwind CDN, Google Fonts) → 배포 빌드 시 인라인/자기완결화.
